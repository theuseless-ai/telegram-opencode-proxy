# secrets — never commit
.ssh_secret
*.secret
*_secret
.env
.env.*
!.env.example

# local runtime config (holds the bot token / chat ids) — commit config.example.toml only
config.toml
config.local.toml

# SQLite store (routing + whitelist + pending pairings/approvals) + WAL sidecars
proxy.db
proxy.db-wal
proxy.db-shm
*.db
*.db-wal
*.db-shm

# rust build
/target

# orchestration scratch
.sisyphus/


# Added by cargo
#
# already existing elements were commented out

#/target
